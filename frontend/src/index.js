import React from 'react';
import ReactDOM from 'react-dom';

import './index.css';
import App from './App';

ReactDOM.render(
  // <React.StrictMode> // Para evitar problemas con react-transition-group
  <App />,
  // </React.StrictMode>,
  document.getElementById('root')
);
